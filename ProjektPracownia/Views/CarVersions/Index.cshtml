@model IEnumerable<ProjektPracownia.Models.CarVersion>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.version)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.carModel)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.version)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.carModel.model)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CarVersionID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CarVersionID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CarVersionID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
